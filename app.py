# app.py — Streamlit dashboard entrypoint (Dev 3)
# Luồng: forecast_demand -> generate_policies -> simulate/run_baseline -> rank_policies
#        -> explain -> Manager Approve/Override.
# Import trực tiếp các hàm từ core/ (KHÔNG REST, KHÔNG FastAPI).
